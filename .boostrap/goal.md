# PomPom bootstrap goal

- [ ] Stop goal: check the complete `libs/` folder with the PomPom bootstrap implementation, run its tests, and check the fuzzy-false tests; finish only when the evidence shows the bootstrap works.
- [ ] Implement the bootstrap in PomPom itself under `boostrap/`.
- [ ] Use `libs/string.pom` for string operations instead of reimplementing strings locally.
- [ ] Split the implementation into focused libraries; add as many libraries as clarity and dependency structure require.
- [ ] Ensure every recursive definition is expressed through `accessibility_less` and/or `recur`, so all bootstrap recursion is well-founded.
- [ ] Constraint correction from user: do not use an arbitrary numeric fuel/budget to justify recursion. Define the actual accessibility relation for each recursive domain, prove its accessibility instance using the same strategy as `libs/acc.pom`, and recurse by eliminating that proof.
- [ ] Preserve the semantics of the existing implementation; adaptations may change structure but not behavior.
- [ ] Insert small proofs throughout the implementation to check invariants and semantics while building it.
- [ ] Preserve unrelated pre-existing worktree changes; current inspection found modified and untracked files in the Haskell implementation and `libs/`.
- [x] Created the explicit persistent goal and this engineering log before implementation work. Motivation: make the acceptance criteria and constraints auditable from the start.
- [ ] Map the current parser, checker, CLI, library dependency order, test commands, and fuzzy-false behavior before choosing the bootstrap module boundaries.
- [ ] Record each discovered bug or missing feature here as a separate unchecked item until fixed and verified.
- [ ] Agent coordination: no sub-agent used initially; direct inspection keeps ownership clear. If one is used later, its task and the review of all its work will be logged here.
- [x] Inspected the repository shape and current worktree before editing implementation files. Motivation: the existing modified/untracked string, accessibility, parser, checker, and CLI work is part of the user's state and must be preserved.
- [ ] Discovery: the native README explicitly says the current Haskell checker does not enforce termination; the PomPom bootstrap must therefore add well-founded traversal structure even where the reference implementation uses ordinary Haskell recursion.
- [ ] Discovery: the present command loads transitive `.pom` imports, reverses the accumulated definition groups, and calls the Haskell `typeCheck`; reproduce this observable import/check order in the bootstrap driver.
- [x] Searched tracked history, branches, and the worktree for a prior bootstrap/fuzzy harness. Motivation: reuse would reduce semantic drift; none exists.
- [ ] Discovery: `libs/acc.pom` is the available well-foundedness foundation (`Bigger`, `Total`, `accesibility_less`, `accesibility_less2`, `recur`). The deleted `libs/terminating.pom` only adds an unfinished `recursion` signature and has no body, so it cannot be reused as a generic eliminator.
- [ ] Define an explicit bootstrap recursion discipline on top of `libs/acc.pom` and prove its decreasing-step obligations; do not rely on the native checker's acceptance of unrestricted self-reference.
- [ ] Establish and record the current native pass/fail result for every `libs/*.pom` file before implementing differential tests.
- [x] Built the native executable successfully with `cabal build`. Motivation: establish that the current reference implementation is runnable before using it as a semantic oracle.
- [x] Ran the native checker on every `libs/*.pom` entrypoint. Motivation: establish the differential baseline—18 files pass; `libs/casting.pom` and `libs/test2.pom` fail in the parser because both end in incomplete definitions (`| A` and `bloom | A`).
- [ ] Acceptance interpretation: checking the complete folder must preserve these two expected parser rejections while accepting all 18 well-formed library entrypoints; do not silently classify malformed sources as successful.
- [ ] Agent coordination: use the one permitted sub-agent for a bounded read-only audit of checker semantics and bootstrap module/test decomposition; make no delegated edits, then independently review its report against the source before adopting it.
- [ ] Architecture decision: keep filesystem I/O plus the existing parser/purifier as a small host bridge, marshal the purified core `Term`/`Local` graph into PomPom data, and compute the acceptance verdict with PomPom code. Motivation: PomPom has no I/O primitive, while this still self-hosts the semantic checker and uses `String` for reference names.
- [ ] Add a generic accessibility-based fold: its public wrapper obtains `Total` with `recur`, and its only recursive worker transports the decreasing proof with `accesibility_less`. Express every later traversal as a non-self-recursive step passed to this fold.
- [ ] Validate the well-founded fold with small reduction/equality proofs before depending on it.
- [x] Proved and type-checked the accessibility foundation in `boostrap/well_founded.pom`: `bootstrap-successor-bigger`, `bootstrap-total`, the generic accessibility fold, and an executable guarded countdown proof all pass. Motivation: later recursive code has a tested `recur`-guard pattern instead of relying on unrestricted recursion.
- [ ] Discovery: the native evaluator does not reduce the higher-order generic fold in closed equality proofs because it avoids unfolding definitions containing a still-stuck match beneath lambdas. Use the proven direct `recur`-guard pattern in computational functions; retain the generic fold as a typed specification, not the execution path.
- [x] Reviewed the sub-agent's interim audit against local source/baseline. Confirmed independently: 18 `*.pom` passes, two parser failures with exit status 0, no fuzzy corpus, `.mini` is a separate unsupported format, no PomPom I/O/result printer, and Unicode identifiers exceed `String`'s ASCII constructors. Motivation: no conclusion is adopted solely on delegation.
- [ ] Harness scope: enumerate `libs/*.pom`; classify by checker output plus timeout rather than exit code; exclude `libs/*.mini` as a different format.
- [ ] Bridge encoding: encode every reference name injectively as an ASCII-safe `String` (for example UTF-8/Unicode hexadecimal text) so Unicode target identifiers remain distinguishable without changing the current string ABI.
- [ ] Object/meta distinction: enforce accessibility only on recursive definitions implementing the bootstrap; preserve the object language's current semantics, which accepts unrestricted recursion in checked target programs.
- [x] Reviewed four sub-agent-reported semantic traps directly in `Checker.hs`/`Term.hs`: match equality compares only the first clause pair; matching selects the last matching source clause because of `foldl` plus cons; parameterized constructor targets are accepted unconditionally and one constructor-check result is discarded; repeated textual pattern binders are purified as distinct fresh variables. Motivation: distinguish intended behavior from native false positives before writing fuzzy negatives.
- [ ] Semantics policy: preserve successful-library behavior and deterministic last-matching-clause reduction, but do not reproduce native false-positive omissions in the bootstrap. Add explicit fuzzy-false cases for differing later clauses, invalid parameterized constructors, and nonlinear/repeated pattern expectations; document any oracle disagreement as a native bug.
- [x] Completed and independently reviewed the sole sub-agent's read-only audit; it edited no files. Motivation: its final feature inventory, negative-test matrix, and bridge limitations were checked against the parser/checker and incorporated selectively; no further agents will be used.
- [x] Implemented and type-checked `boostrap/name.pom` using `libs/string.pom`, exhaustive character coding, and a `recur`-guarded string traversal. Closed proofs cover empty, equal, and unequal names. Motivation: establish collision-free string identity for the core environment.
- [x] Implemented and type-checked `boostrap/data.pom`, splitting core terms, clauses, variables, bindings, contexts, results, steps, inputs, and verdicts into explicit PomPom datatypes. Motivation: give the bridge and checker a typed, host-independent semantic representation.
- [x] Implemented and type-checked `boostrap/term_equality.pom`; every mutual traversal consumes the accessibility witness of a strict syntax child. Closed proofs cover equal variables, unequal variables, and matches whose difference occurs after the first clause. Motivation: fix the native first-clause-only conversion false positive at the foundational equality layer.
- [x] Implemented and type-checked `boostrap/collections.pom` with guarded environment/context lookup, static-versus-expression filtering, root lookup, and universe/name constants. Closed lookup and missing-key proofs pass. Motivation: replace host maps with auditable PomPom association lists.
- [x] Retired the prototype recursion ledger that used Nat fuel. Motivation: the user's correction requires accessibility of the actual recursive domain, so none of those fuel-indexed operations remains in the bootstrap implementation.
- [ ] Refactor required: the completed name, term equality, collection lookup, and substitution modules currently use Nat fuel as their recursive index. Motivation: the user clarified that a fuel-like approach is not acceptable even when guarded by `recur`; replace these APIs and proofs with structural accessibility instances before building normalization/checking.
- [ ] Recursion annotation format for every recursive operation: domain; strict relation; accessibility constructor/instance; recursive call's relation witness; executable proof showing the intended equation.
- [ ] Define a generic PomPom `Acc` family and eliminator modeled on `libs/acc.pom`, then prove structural accessibility for `String`, polymorphic `List`, `BootstrapTerm`, and mutually nested term/clause lists.
- [x] Replaced the Nat-fuel prototype with `BootstrapAccessibility` and its eliminator in `boostrap/well_founded.pom`. Motivation: recursion now consumes an inductive accessibility proof instead of an arbitrary evaluation budget.
- [x] Proved `BootstrapStringSmaller` as the actual tail relation and `bootstrap-string-accessibility` for every `String`; refactored name equality to recurse only with the descendant proof. Equal/unequal/empty closed proofs pass.
- [x] Proved structural accessibility instances for generic lists plus the mutually nested `BootstrapTerm`, term-list, clause, and clause-list shapes in `boostrap/structure_accessibility.pom`; added closed instance proofs.
- [x] Refactored term equality into one work-indexed recursion over the structural accessibility witnesses. Motivation: every term/list/clause back-edge consumes an accessibility field from the parent proof; the later-clause false-positive proof still passes without fuel.
- [x] Refactored binding/context lookup and root traversal to use specialized binding-list, typing-list, and term accessibility instances. Motivation: the native dependent matcher could not reliably reduce the polymorphic list proof at a closed type, so the specialized instances make the same relation explicit and executable.
- [x] Refactored substitution into one tagged work recursion whose term/list/clause branches carry their structural accessibility witnesses. Variable replacement and lambda-shadowing proofs pass without fuel.
- [x] Resolved evaluator constraint: recursive entrypoints package data and its accessibility witness into a single first argument. Motivation: the native stage-0 evaluator only unfolds a match-driven function reliably when its discriminant becomes known at the first beta step; this packaging preserves structural recursion and enables closed proofs.
- [x] Recursion annotation—name equality: domain `String`; relation `BootstrapStringSmaller left tail`; instance `bootstrap-string-accessibility`; back-edge witness is `descend tail (refl String tail)`.
- [x] Recursion annotation—term equality/substitution: domain is the mutually nested term/clause/list syntax tree; instances are `bootstrap-*-accessibility-instance`; every back-edge receives the exact child accessibility stored by the parent constructor.
- [x] Recursion annotation—environment lookup: domain is the concrete binding/typing list; relation is strict tail; instances are `bootstrap-binding-list-accessibility-instance` and `bootstrap-typing-list-accessibility-instance`; each back-edge uses the stored tail proof.
- [x] Implemented and type-checked `boostrap/matching.pom`: pattern comparison recurses over pattern accessibility, branch substitution recurses over the same pattern proof, and clause selection consumes clause-list accessibility. Closed proofs cover constructor-spine matching, binder substitution, and the native-compatible last-clause priority.
- [x] Normalization design: define a structurally recursive, total one-step reducer; represent multi-step normalization by a finite reduction-path certificate; verify every adjacent step and the final normal-form claim in PomPom while consuming the path's structural accessibility instance. Motivation: beta/unfold steps can enlarge syntax and unrestricted object recursion prevents proving a universal normalization instance; an arbitrary Nat fuel is forbidden and would not prove termination.
- [x] Clarification: a reduction path is evidence, not fuel—its entries are semantic terms, every edge must equal the bootstrap's deterministic one-step result, the final term must be irreducible, and missing/extra/invalid evidence rejects rather than granting a number of unchecked attempts.
- [x] Implemented and type-checked `boostrap/reduction.pom`, a total deterministic one-step reducer. Closed proofs verify beta substitution and expression-binding unfolding. Motivation: every recursive descent is structural even though a semantic reduction step may grow the resulting term.
- [x] Recursion annotation—one-step reduction: domain `BootstrapTerm`; relation is the strict mutually nested syntax-child relation; instance is `bootstrap-term-accessibility-instance`; all recursive calls use the child accessibility field exposed by the matched parent proof. Substitution, matching, and environment lookups are invoked through their independently proved accessibility instances.
- [x] Implemented and type-checked `boostrap/normalization.pom`: it consumes a structurally accessible list of expected successor terms, recomputes each deterministic step, checks exact term equality, and accepts only when the last term is irreducible. Motivation: valid, missing, wrong, and extra-edge closed proofs all pass, demonstrating that the path is checked evidence rather than execution fuel.
- [x] Recursion annotation—normalization verification: domain is `List BootstrapTerm`; relation is strict tail; instance is `bootstrap-term-list-accessibility-instance`; every back-edge receives `tail-access` from `bootstrap-term-list-access-new`. No numeric counter or budget appears in the implementation.
- [x] Added `boostrap/certificates.pom`, with a structurally accessible certificate database indexed by exact start term. Irreducible terms need no entry; reducible terms fail closed when their certificate is absent or invalid. Closed proofs cover valid lookup, no-certificate normal terms, and missing-certificate rejection.
- [x] Recursion annotation—certificate lookup: domain is `List BootstrapNormalizationCertificate`; relation is strict tail; instance is `bootstrap-certificate-list-accessibility-instance`; lookup's only back-edge receives the stored tail witness.
- [ ] Supply the checked certificate database to conversion/inference through the bootstrap check configuration and make missing evidence an explicit rejection.
- [x] Implemented and type-checked `boostrap/annotation_erasure.pom`, structurally removing transparent notation annotations across terms and nested lists/clauses. Motivation: conversion can compare computational bodies without relying on native annotation quirks.
- [x] Implemented and type-checked `boostrap/alpha_equality.pom`, including binder renaming for Pi and lambda terms and complete clause-list comparison. Closed proofs accept alpha-renaming and reject a genuinely free/captured-variable mismatch.
- [x] Implemented and type-checked `boostrap/conversion.pom`: both sides must have valid certified normalization, annotations are erased, alpha-equivalence is checked, and constructor-set downgrade remains directional. Proofs cover beta conversion, alpha conversion, allowed downgrade, rejected upgrade, and missing-certificate rejection.
- [x] Recursion annotation—annotation erasure and alpha equality: domains are the mutually nested term/clause/list syntax trees; instances are the `bootstrap-*-accessibility-instance` family; every back-edge consumes the corresponding child witness. Binder renaming invokes the separately structural substitution traversal.
- [x] Added and type-checked `boostrap/constructor_membership.pom`: constructor membership is based on the exact static root binding and structurally scans the declared family. Closed proofs accept an applied member and reject a different static constructor. Motivation: this is the first half of sound constructor-family checking and avoids the native unconditional parameterized-target shortcut.
- [ ] Combine constructor membership with inferred/normalized return-type compatibility, so a same-name or parameterized constructor cannot pass against the wrong family instance.
- [x] Implemented and type-checked `boostrap/occurs.pom`: occurrence checks inspect the complete mutually nested term/clause tree, including the whole term itself. Present/absent closed proofs pass.
- [x] Implemented and type-checked `boostrap/unification.pom`: substitutions are applied by a structurally accessible unifier-list fold, equal-headed applications decompose through a strict argument child, insertion orients bound variables consistently, and occurs checks prevent cyclic entries. Closed proofs cover variable solving, application decomposition, and cycle rejection.
- [x] Recursion annotation—occurs/unification: occurrence traversal uses syntax-child accessibility; unifier application uses strict list-tail accessibility; application unification recurses only to the target argument witness stored by its parent. Insertion itself is non-recursive and reuses those proved traversals.
- [x] Added and type-checked `boostrap/typing_data.pom`, `boostrap/type_lookup.pom`, and `boostrap/typing_helpers.pom`. Motivation: source bindings and normalization certificates remain immutable, while local typings, unifier entries, and learned types evolve explicitly; lookup proofs cover universes and static declarations.
- [x] Added and type-checked `boostrap/pattern_unification.pom`: pattern-bound variables are introduced with expected domains, `_` is the explicit pattern wildcard, repeated type occurrences are unified consistently, and constructor-family targets are normalized before comparison. A closed repeated-variable constraint proof reduces to a successful environment result.
- [x] Added and type-checked the first integrated bidirectional checker in `boostrap/typing.pom`. Closed proofs infer a Pi type, check an alpha-renamed identity lambda, and reject application of a non-function.
- [x] Removed the provisional same-term Pi phase call immediately after discovering it. Motivation: Pi inference/checking now share a non-self-recursive helper whose only calls back into the checker are the strict domain and body children.
- [x] Recursion annotation—integrated typing: the work domain is the disjoint structural family of term, constructor-list, and clause-list nodes; term calls consume child witnesses, list calls consume tail witnesses, and Pi helper calls return only through domain/body child witnesses. Pattern unification consumes the pattern-type syntax tree.
- [x] Applied accumulated pattern unifier substitutions before ordinary body conversion. Motivation: a closed dependent proof reduces `P x` and `P y` to the same type only after consuming the stored `x ↦ y` equation.
- [x] Implemented and type-checked `boostrap/driver.pom`: the binding-list driver resets local state per declaration, validates static declarations as types, learns expression types only after successful checking, preserves immutable bindings/certificates, and maps parse errors or check failures to rejection. Closed miniature programs prove acceptance and rejection.
- [x] Recursion annotation—module driver: domain is the pending `List BootstrapBinding`; relation is strict tail; instance is `bootstrap-binding-list-accessibility-instance`; the sole back-edge receives `tail-access` from the matched list constructor.
- [ ] Implement the host parser/purifier bridge, injective name encoding, normalization-certificate production, and a runner that evaluates the bootstrap verdict rather than the native checker verdict.
- [x] Added a separate `KeiBootstrap` executable in `boostrap/BootstrapMain.hs`; `cabal build all` succeeds. It reproduces import loading/purification, densely remaps host-only random binder IDs, encodes every reference as UTF-8 hexadecimal `String`, and emits a PomPom proof harness. It never imports or calls native `Checker.typeCheck`.
- [ ] Bridge test discovery: the first generated `libs/logic` harness evaluates the bootstrap verdict to rejection with an empty certificate database. Narrow the first failing declaration before changing checker behavior; likely causes are constructor-family validation or a missing normalization certificate.
- [x] Narrowed the empty-certificate failure to `second`, whose type normalizes the expression alias `AxB`; the preceding static declarations pass. Motivation: this confirmed missing normalization evidence rather than silently weakening typing rules.
- [x] Added a string-backed bound-variable constructor for bridged host identifiers while retaining Nat-backed binders for internal proofs. Updated every variable case and reran the complete bootstrap proof suite. Motivation: dense per-definition Nat remapping could collide after unfolding and unary global IDs would be impractical; exact source IDs in `String` preserve identity without fuel or lossy compression.
- [x] Added host-side normalization-path discovery mirroring the structurally total PomPom one-step reducer. It emits a certificate only when reduction reaches an irreducible term; exact cycles emit no certificate. The PomPom verifier still recomputes every edge and rejects missing/invalid evidence.
- [x] Optimized certificates without weakening verification: lookup first compares a structurally computed root-name key, candidates are restricted to pending declarations' type positions, and redundant suffix certificates are omitted because each full path already verifies its suffix edges.
- [x] Whole-library milestone: generated and checked the complete `libs/logic` harness (81 KB, 22 certificates); the PomPom bootstrap driver returned acceptance and its closed proof passed. Motivation: this is the first complete library accepted by the bootstrap rather than by the native checker oracle.
- [ ] Performance bug: complete `libs/logic` evaluation takes roughly 3.5 minutes. Build a declaration-sharded runner with complete source bindings and minimal certificates, while retaining the full-driver check as an integration test.
- [x] Corrected bridge declaration order to match the native `foldr` checker: immutable lookup bindings retain native map precedence, while pending declarations run in effective source/dependency order. Added `current` and offset/limit selections for dependency-complete sharding.
- [x] Current-module shards pass for `function`, `functor`, `maybe`, and `monad`.
- [x] Fixed dependent match-body typing exposed by `libs/k.pom`: after checking a pattern against the scrutinee type, the checker now also unifies the pattern term with the scrutinee term before checking the body. Motivation: this turns `T e` into `T (refl A a)` in the `axiom_k` branch; the complete `k` shard now passes.
- [x] Current-module shard for `libs/test.pom` passes. Motivation: the empty-module edge case still constructs and proves an accepting bootstrap driver verdict.
- [x] Recovered `libs/bits.pom` validation after an interrupted first attempt: reran the same generated harness to completion and classified the exact `Kei checked your file with successful` marker. Motivation: only the completed run is evidence; `bits` is now a bootstrap-confirmed pass.
- [ ] Harness rule: process timeouts are operational safeguards for the host runner only. They are not exposed to PomPom, do not bound normalization, and are not semantic fuel; the accepted verdict must still be justified by structural accessibility plus checked reduction-path evidence.
- [x] Final `libs/bool.pom` validation passed through the reusable runner: shards `[0,4)`, `[4,8)`, and `[8,9)` all reached the exact success marker, and the JSON module aggregation is `true`. Motivation: their union covers all nine current declarations under final code while avoiding the combined harness's non-semantic 20-minute host timeout.
- [x] Extended bridge selection with `current OFFSET LIMIT` and rejected negative limits/offsets. Motivation: declaration-level diagnosis now keeps the complete immutable import environment while minimizing the checked pending set; invalid slice arguments can no longer silently select surprising data.
- [x] Replaced long UTF-8-hex user references and raw random binder IDs in generated harnesses with deterministic, injective, harness-local `String` interning (`rN` references and `bN` binders), while preserving the bootstrap ABI encodings for `*`, `Type`, `Kind`, `__`, and `_`. Motivation: equality still uses `libs/string.pom`, Unicode names remain distinct through the one-to-one table, distinguished syntax keeps its proven identity, and shorter structural strings substantially reduce proof normalization cost. An initial validation caught the missing distinguished-name exception; the mapping was corrected before adopting interned harness results.
- [x] Revalidated the corrected interned bridge on the known-good complete `libs/function.pom` current-module shard; its closed proof passed. Motivation: demonstrate that shortening names preserves bootstrap semantics before using the optimization for further library evidence.
- [ ] Certificate-coverage bug: both final dependent proof declarations in `libs/bool.pom` reject, while its first seven datatype/computation declarations pass. Likely cause: conversion applies match unifiers to a result type, but the bridge only certified the unsubstituted source type. Generate and verify branch-instantiated result-type certificates; retain missing-certificate rejection itself.
- [x] Resolved the earlier `libs/equality.pom` rejection and reran its complete current-module shard to the exact success marker. Motivation: branch-local equations plus synthesized application-result certificates cover `rewrite'`, `cong`, `symmetry`, and `neg_eq_symmetry`; the result is a whole-module bootstrap proof, not inferred from the passing prefix.
- [x] Added match-branch certificate candidates by structurally substituting each source pattern for the scrutinee in the declared result type. Motivation: these are precisely the dependent expected types produced after the bootstrap records a pattern/scrutinee equation; they remain concrete checked reduction paths, not a numeric allowance.
- [x] Found and fixed sibling-clause state leakage in `boostrap/typing.pom`: after a branch body succeeds, the next clause now starts from the saved pre-branch environment instead of inheriting the prior pattern equation. Motivation: the native checker saves/restores its unifier around each branch, and dependent branches such as `true`/`false` require mutually incompatible scrutinee equations.
- [x] Extended certificate discovery with a read-only host type skeleton: it derives application codomains by substituting actual arguments into known Pi types, follows lambda annotations, tracks Pi binders, and records match-instantiated expected types. Motivation: conversion normalizes inferred types such as `Eq Bool true true` that need not occur literally in source; each emitted path is still independently recomputed and proved by PomPom.
- [x] Added a closed two-branch dependent-match regression proof in `boostrap/typing.pom`: sibling branches require incompatible equations `x ↦ c` and `x ↦ d`, and both bodies type-check only if branch-local unifiers are isolated. Motivation: make the state-reset fix executable inside PomPom rather than relying only on the external `bool` symptom.
- [x] Added `boostrap/run_tests.py`, a reusable classifier for internal proofs, every `libs/*.pom` entrypoint, and the fuzzy-false corpus. Motivation: it builds both executables, preserves per-case harness/log/elapsed/size evidence, treats the two known malformed libraries as expected rejections, requires the exact success marker because the stage-0 CLI can exit zero on errors, and labels its process timeout explicitly as host safety rather than semantic fuel.
- [ ] Runner scalability: add deterministic current-module declaration sharding and module-level aggregation. Motivation: certificate lookup in one combined `bool` harness exceeded the host safety window even though every isolated declaration passed; sharding changes only proof packaging, retaining the full immutable environment and covering every declaration exactly once.
- [x] Implemented deterministic accepted-library sharding in the runner (default four current-module declarations per harness), driven by bridge-reported declaration counts and aggregated back to module status. Motivation: every shard retains the complete immutable import/binding environment, the union covers each current declaration exactly once, malformed/negative cases remain unsharded, and no shard size is exposed to PomPom recursion or normalization.
- [x] Runner smoke test passed for sharded `libs/function.pom` and `libs/functor.pom`; the JSON summary aggregated both modules to `true` and preserved their exact success-marker logs. Motivation: validate count discovery, shard selection, concurrent classification, and module aggregation before launching the full folder suite.
- [x] Added an initial `boostrap/fuzzy_false/` corpus covering non-function application, wrong annotation, wrong parameterized constructor family, repeated source pattern binders, a type difference hidden in a later match clause, and malformed syntax. Motivation: target the known native false-positive surfaces plus ordinary rejection paths instead of testing only happy cases.
- [x] Ran all six fuzzy-false cases through the final runner; every case reached its declared bridge layer and its closed rejection proof passed. Motivation: verified semantic rejection for wrong application, wrong annotation, wrong parameterized constructor family, and later-clause mismatch; separately verified the repeated-binder guard and intentional parser failure. Summary: `/tmp/bootstrap-final-fuzzy/summary.json`.
- [x] Invalid/missing/extra reduction evidence remains covered by closed proofs in `boostrap/normalization.pom` and `boostrap/certificates.pom`. Motivation: source-level fuzzy files cannot inject the bridge's certificate object directly, so these internal negative proofs exercise the trustworthy verifier boundary without adding an unsafe external certificate syntax.
- [x] Made bridge rejection provenance explicit (`parsed`, `parse-error`, or `repeated-pattern-binder`) and required it in the runner. Motivation: a semantic fuzzy-negative cannot be counted merely because malformed syntax also reduces to rejection; each test must reach its intended layer.
- [x] Removed the bridge reducer's unreachable nested-notation match alternative after GHC identified it as redundant. Motivation: the preceding outer `Match (Notation ...)` case already handles exactly that shape, so eliminating dead code keeps the host mirror auditable.
